'use client';import Link from 'next/link';import {useState} from 'react';export default function Login(){const[message,setMessage]=useState('');return <main><section className="section" style={{paddingTop:160,minHeight:'80vh'}}><div style={{maxWidth:500,margin:'auto'}} className="form-card"><div className="eyebrow">Guest account</div><h1 className="serif" style={{fontSize:44}}>Welcome back.</h1>{message&&<div className="success">Demo sign-in successful. Connect your authentication provider for production accounts.</div>}<form onSubmit={e=>{e.preventDefault();setMessage('ok')}}><div className="field"><label>Email</label><input type="email" required/></div><div className="field" style={{marginTop:18}}><label>Password</label><input type="password" required/></div><button className="btn btn-dark" style={{width:'100%',marginTop:22}}>Sign in</button></form><p className="muted" style={{marginTop:20}}>New guest? <Link href="/register" className="gold">Create an account</Link></p></div></section></main>}
