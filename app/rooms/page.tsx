import type {Metadata} from 'next';import RoomCard from '../../components/RoomCard';import {rooms} from '../../lib/data';export const metadata:Metadata={title:'Rooms & Suites',description:'Explore Luxora Grand Hotel rooms, suites, villas and residences.'};export default function Rooms(){return <main><section className="page-hero"><div className="container"><div className="eyebrow">Stay beautifully</div><h1 className="serif">Rooms & Suites</h1><p>Choose your pace. From intimate city rooms to expansive private residences, every stay is designed around comfort.</p></div></section><section className="section"><div className="container"><div className="grid rooms-grid">{rooms.map(r=><RoomCard room={r} key={r.id}/>)}</div></div></section></main>}
