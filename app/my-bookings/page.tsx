import type {Metadata} from 'next';import Link from 'next/link';export const metadata:Metadata={title:'My Bookings'};export default function MyBookings(){return <main><section className="section" style={{paddingTop:160,minHeight:'75vh'}}><div className="container" style={{maxWidth:900}}><div className="eyebrow">Guest area</div><h1 className="serif" style={{fontSize:52}}>My bookings.</h1><div className="form-card" style={{marginTop:30}}><div style={{display:'flex',justifyContent:'space-between',gap:20,flexWrap:'wrap'}}><div><div className="eyebrow">Demo booking</div><h2 className="serif" style={{fontSize:30}}>No active reservations yet</h2><p className="muted">Your confirmed stays will appear here after connecting the booking backend.</p></div><Link href="/booking" className="btn btn-gold">Book a stay</Link></div></div></div></section></main>}
