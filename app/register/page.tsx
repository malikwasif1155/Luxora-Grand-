'use client';import Link from 'next/link';import {useState} from 'react';export default function Register(){const[done,setDone]=useState(false);return <main><section className="section" style={{paddingTop:160,minHeight:'80vh'}}><div style={{maxWidth:550,margin:'auto'}} className="form-card"><div className="eyebrow">Guest account</div><h1 className="serif" style={{fontSize:44}}>Create your account.</h1>{done?<div className="success">Demo account created. In production, connect this form to your auth/database provider.</div>:<form onSubmit={e=>{e.preventDefault();setDone(true)}}><div className="form-grid"><div className="field"><label>First name</label><input required/></div><div className="field"><label>Last name</label><input required/></div><div className="field full"><label>Email</label><input type="email" required/></div><div className="field full"><label>Password</label><input type="password" minLength={8} required/></div></div><button className="btn btn-gold" style={{width:'100%',marginTop:22}}>Create account</button></form>}<p className="muted" style={{marginTop:20}}>Already a guest? <Link href="/login" className="gold">Sign in</Link></p></div></section></main>}
