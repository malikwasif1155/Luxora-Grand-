import Link from 'next/link';import {ArrowUpRight} from 'lucide-react';import type {Room} from '../lib/types';
export default function RoomCard({room}:{room:Room}){return <article className="card"><div className="room-img" style={{backgroundImage:`url(${room.image})`}}><span className="price-tag">From ${room.price} / night</span></div><div className="card-body"><div className="eyebrow">{room.type}</div><h3>{room.name}</h3><p className="muted">{room.description}</p><div className="features"><span>{room.size}</span><span>{room.beds}</span><span>Up to {room.guests} guests</span></div><Link className="btn btn-outline dark" href={`/rooms/${room.id}`}>View room <ArrowUpRight size={16}/></Link></div></article>}
