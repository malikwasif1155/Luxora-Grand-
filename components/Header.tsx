'use client';
import Link from 'next/link';import {useState} from 'react';import {Menu,X} from 'lucide-react';
export default function Header(){const[open,setOpen]=useState(false);const links=[['Rooms','/rooms'],['About','/about'],['Gallery','/gallery'],['Contact','/contact']];return <header className={'nav '+(open?'open':'')}><div className="container nav-inner"><Link href="/" className="brand" onClick={()=>setOpen(false)}><span className="brand-mark">LG</span>LUXORA GRAND</Link><nav className="nav-links">{links.map(([x,y])=><Link key={y} href={y} onClick={()=>setOpen(false)}>{x}</Link>)}<Link href="/my-bookings" onClick={()=>setOpen(false)}>My Booking</Link><Link href="/booking" className="btn btn-gold" onClick={()=>setOpen(false)}>Book Now</Link></nav><div className="nav-actions"><Link href="/booking" className="btn btn-gold mobile-only">Book Now</Link><button className="menu" aria-label="Toggle menu" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></div></header>}
